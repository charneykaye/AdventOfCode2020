#include<iostream>
#include <fstream>
#include <bits/stdc++.h>

using namespace std;

/*
Bootstrap reads inputs from file
*/
int main()
{
    vector<string> lines;
    vector<int> amounts;
    string line;
    while (std::getline(std::cin, line)) 
        theorems.push_back(line);

    return 0;
}
